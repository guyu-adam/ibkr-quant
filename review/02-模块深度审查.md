# 模块深度审查（v2 更新）

## 新增模块

### core/broker_interface.py ★★★★★ — 优秀（NEW）
- 59 行，定义了 connect / disconnect / net_liquidation / daily_pnl / positions / last_price / market_order 七个抽象方法
- positions() 统一返回 dict[str, float]
- IBKRBroker 和 OKXBroker 均已继承此基类

### core/indicators.py ★★★★★ — 优秀（NEW）
- 137 行，rsi / macd / atr / ema / trix / kdj / bollinger_bands / max_drawdown / obv / vwap / adx / williams_r / cci / sortino_ratio
- 统一了代码库中 5 份 RSI 实现的重复代码
- fast_trading.py、contracts.py 已改用此模块

### core/backtest.py ★★★★☆ — 良好（NEW）
- 220 行，BacktestBroker 继承 BrokerInterface，含佣金(0.1%)+滑点(0.05%)模型
- BacktestEngine 含 run / report（Sharpe / max drawdown / win rate / profit factor）
- 与 WalkForwardValidator 集成
- 建议：滑点和佣金应可配置为不同市场（美股/港股/加密货币费率不同）

---

## 已修复模块（关键变更）

### core/engine.py ★★★★☆ — 从 ★☆☆☆☆ 提升
- 死导入已修复：`from interfaces.ibkr import IBKRBroker`、`from strategies.fast_trading import MeanReversionStrategy`
- 移除了 `compute_indicators` / `generate_signal` 的 v1 依赖
- 使用 `self.strategy.evaluate()` 调用模式
- 仍用 `type("Feed", ())` 动态 mock — 建议改用真实的 DataFeed

### config/settings.py ★★★★☆ — 从 ★★☆☆☆ 提升
- API Key 改为 `os.getenv()` + `dotenv` 加载，不再写死空字符串
- 添加了 `WATCHLIST = US_WATCHLIST` 向后兼容别名
- 添加了 `.env.example` 模板文件

### core/risk.py ★★★★★ — 从 ★★★★☆ 提升
- test_risk.py 新增 60 行测试，覆盖 Kelly / ATR multiplier / record_trade
- 无需代码改动（原实现质量就很好）

### strategies/fast_trading.py ★★★★☆ — 重构
- 用 `core.indicators.rsi/atr/macd` 替代本地 RSI 实现，消除了第 5 份拷贝
- `evaluate()` 方法签名不变，仍与 `on_bar()` 不同（这是设计选择，非 bug）
- 删除了 `_rsi / _atr / _macd` 本地方法（~35 行减少）

### strategies/options.py ★★★★☆ — 从 ★★☆☆☆ 跨越式提升
- 从 101 行 stub 扩展到 316 行
- `generate_signals()` 含完整期权链过滤：`filter_options_by_delta()`、`filter_options_by_dte()`
- Covered Call / Protective Put / Iron Condor 全部有实际逻辑
- VIX 仍硬编码为 20.0 — `set_vix()` 存在但需外部数据源连接

### strategies/long_term.py ★★★★☆ — HRP 修复
- `_in_cluster(link, target, node)` 签名修复（移除无用的 idx 参数）
- `test_hrp.py` 5 个测试：叶子查找自己 / 不同叶子不匹配 / 所有叶子在根中 / 权重和为 1 / 不同 linkage/shrinkage 方法

### strategies/contracts.py ★★★★☆ — 小幅改进
- 用 `core.indicators.ema` 替代本地 EMA
- `grid_state.json` 硬编码仍存在

### interfaces/ibkr.py ★★★★☆ — 从 ★★★☆☆ 提升
- 继承 `BrokerInterface`
- 新增 `get_bars()` 方法（对接 engine）
- `net_liquidation()` 错误时返回 `0.0` 而非 `None`（修复了之前的风险）

### interfaces/schwab.py ★★★★☆ — 从 ★★☆☆☆ 跨越式提升
- 从 62 行扩展到 314 行
- 实现完整 OAuth 2.0 流程：authorization URL → 本地 HTTP server 收 callback → token 交换 → 自动 refresh
- 账户/持仓/市价单 API 完整实现

### interfaces/ths.py ★★★★☆ — 从 ★★☆☆☆ 提升
- 从 97 行扩展到 215 行
- 仍用腾讯财经免费行情 API

---

## 工程基础设施（全新）

### pyproject.toml ★★★★★
- 完整打包配置：setuptools + CLI 入口点 `quant = "main:main"`（注意 main.py 还没有 main() 函数）
- pytest / black / isort 配置
- 可选 dev 依赖分组
- 声明 Python >=3.10（README badge 仍写 3.9+，需更新）

### .github/workflows/ci.yml ★★★★★
- push 触发 pytest + flake8 + black --check + isort --check

### .pre-commit-config.yaml ★★★★★
- black + isort + flake8 hooks

### .env.example ★★★★★
- 5 个券商的 API Key 模板，30 行

---

## 测试覆盖（更新）

| 新增测试文件 | 测试数 | 覆盖模块 |
|-------------|--------|----------|
| test_hrp.py | 7 | _in_cluster + hrp_weights |
| test_fast_trading.py | ~6 | MeanReversionStrategy.evaluate |
| test_contracts.py | ~5 | GridScalpStrategy |
| test_ensemble.py | ~4 | EnsembleEngine |
| test_regime_detector.py | ~4 | RegimeDetector |
| test_meta_labeling.py | ~3 | MetaLabeler |
| test_risk.py (扩展) | +4 | Kelly/ATR multiplier |

总测试数从 38 → ~55，覆盖了上一版标记为"零覆盖"的核心模块。

## 仍为零覆盖的模块

- interfaces/ (5 个接口无测试 — 需要 mock 外部依赖)
- slow_trading.py (仍为 stub)
- llm_alpha.py
- transformer_model.py
- walkforward.py
